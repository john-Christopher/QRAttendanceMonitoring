import React from 'react';

import firestore from '@react-native-firebase/firestore';
import { Text, View } from 'react-native';

function Attendance(qr) {
  <View>
    <Text>{{qr}}</Text>
  </View>
}